[package]
org = "urlshortener"
name = "url_shortener"
version = "0.1.0"

[build-options]
observabilityIncluded = true

[b7a.observability.metrics]
enabled = true

[b7a.observability.metrics.prometheus]
host = "0.0.0.0"
port = 9796      # default exporter port
